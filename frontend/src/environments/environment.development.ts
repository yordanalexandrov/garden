import type { FrontendEnvironment } from './environment';

export const environment: FrontendEnvironment = {
  production: false,
  apiBaseUrl: '/api/v1',
  supabaseAuthUrl: '',
  supabaseAnonKey: '',
};
