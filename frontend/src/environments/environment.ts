import type { FrontendEnvironment } from './environment.model';

export const environment: FrontendEnvironment = {
  production: false,
  apiBaseUrl: '/api/v1',
  supabaseAuthUrl: '',
  supabaseAnonKey: '',
};
